[package]
name = "ArgentumCoopBevy"
version = "0.1.0"
edition = "2024"

[dependencies]
nano-id = "0.4.0"
rand = "0.9.2"
strum = "0.27.2"
strum_macros = "0.27.2"
vec-collections = "0.4.3"
debug_unwraps = "0.1.0"
rand_distr = "0.5.1"
rand_seeder = "*"
rand_pcg = "*"

bevy = { version = "0.16.1", features = [
    "dynamic_linking",
    "track_location",
    "serialize",
    "jpeg",
    #"bevy_remote",
    #.add_plugins(RemotePlugin::default()).add_plugins(RemoteHttpPlugin::default().with_port(port))
] }
superstate = { git = "https://github.com/KurlykovDanila/Superstate.git" }

bevy_replicon = "0.34.3"
bevy_replicon_renet = { git = "https://github.com/projectharmonia/bevy_replicon_renet.git" }
serde = { version = "1.0.219", features = ["derive"] }

bevy_asset_loader = "0.23.0"
bevy-inspector-egui = "0.32.0"
#splines = { version = "5.0.0", features = ["serde"] }
#bevy_northstar = "0.3.1"

[dependencies.fastnoise-lite]
version = "*"

#log = { version = "*", features = ["max_level_debug", "release_max_level_warn"] } # Improve Runtime Performance (Optional) 

# Enable a small amount of optimization in the dev profile.
[profile.dev]
opt-level = 1

# Enable a large amount of optimization in the dev profile for dependencies.
[profile.dev.package."*"]
opt-level = 3


